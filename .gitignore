# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
cmd/hugothemesitebuilder/hugothemesitebuilder
cmd/hugothemesitebuilder/build/site/content/themes
cmd/hugothemesitebuilder/build/site/public
cmd/hugothemesitebuilder/build/.vscode/settings.json
.hugo_build.lock
